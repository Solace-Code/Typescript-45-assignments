// Assignment 04 Famous Quotes 2 //

// Storing the name of the author //

const authorName = "Lao Tzu ";

// Storing the phrase //

const phrase = "once said ";

// Storing Quote //

// we are using single quotation to store string so we can give the quote string it self double quotation //

const quote = '"The journey of a thousand miles begins with one step."'


// Printing out the quote //

console.log(authorName + "" + phrase + quote);