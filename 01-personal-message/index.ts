// Assignment 01 Personal Message //

// Storing the name of the person //

const userName = "Ahmed";

// Writing the message //

const message = "Hello";

// Creating the variable to concatenate the previous two variables //

let announce = message + " " + userName;

// Printing out the concatenate variable //

console.log(announce);